{
    "name": "@pulsarjs/agent",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "esbuild src/index.js --bundle --minify --outfile=dist/agent.js --sourcemap --format=iife",
        "build:dev": "esbuild src/index.js --bundle --outfile=dist/agent.dev.js --sourcemap --format=iife",
        "test": "vitest run"
    },
    "devDependencies": {
        "esbuild": "^0.27.3",
        "vitest": "^4.0.18"
    }
}